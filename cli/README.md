When starting a new session, first read `cli/ACTUAL_CLI.md` — it contains the quoting workaround, category/account IDs, and command patterns needed to use the Actual Budget CLI on this Windows setup.
